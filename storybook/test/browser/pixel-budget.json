{
  "_comment": [
    "Per-example pixel budgets for pixel.spec.mjs. Three maps, and an example",
    "must appear in exactly one of them or in none.",
    "",
    "budgets \u2014 the most differing pixels this example may have and still pass.",
    "  An example in none of these maps gates at zero. A budget is set from a",
    "  measured maximum, never guessed, and is capped at 0.5% of the image.",
    "  A budget more than ten times its observed difference FAILS: a slack",
    "  budget is a stale budget, and the fix is to lower the number rather than",
    "  to leave a blanket over the example.",
    "",
    "pending \u2014 not yet gated. Still runs, still prints its observed difference,",
    "  and is reported as pending rather than as passing. This is how the check",
    "  lands without a 66-way red build: the first run is a census, not a",
    "  verdict.",
    "",
    "skips \u2014 will not be compared, each with the reason. A skipped example is",
    "  still executed, and if it would now pass at zero the run FAILS with",
    "  'remove the skip'. A skip that outlives its reason is the tolerance",
    "  problem wearing a different coat."
  ],
  "budgets": {
    "accordion.default": 0,
    "accordion.multiple": 0,
    "accordion.states": 0,
    "alert-dialog.default": 0,
    "alert.default": 0,
    "aspect-ratio.default": 0,
    "attachment.default": 0,
    "avatar.default": 0,
    "badge.variants": 0,
    "breadcrumb.default": 0,
    "bubble.default": 0,
    "button-group.default": 0,
    "button.sizes": 0,
    "button.variants": 0,
    "calendar.default": 0,
    "card.default": 0,
    "carousel.default": 0,
    "checkbox.default": 0,
    "collapsible.default": 0,
    "combobox.default": 0,
    "command.default": 0,
    "context-menu.default": 0,
    "dialog.default": 0,
    "drawer.default": 0,
    "dropdown-menu.default": 0,
    "empty.default": 0,
    "field.default": 0,
    "hover-card.default": 0,
    "input-group.default": 0,
    "input-otp.default": 0,
    "input.default": 0,
    "item.default": 0,
    "kbd.default": 0,
    "label.default": 0,
    "marker.default": 0,
    "menubar.default": 0,
    "message-scroller.default": 0,
    "native-select.default": 0,
    "navigation-menu.default": 0,
    "pagination.default": 0,
    "popover.default": 0,
    "progress.default": 0,
    "questionnaire.default": 0,
    "radio-group.default": 0,
    "resizable.default": 0,
    "scroll-area.default": 137,
    "select.default": 0,
    "separator.default": 0,
    "shadcn-message.default": 0,
    "sheet.default": 0,
    "sidebar.default": 0,
    "skeleton.default": 0,
    "slider.default": 0,
    "slider.range": 0,
    "snippet.default": 0,
    "sonner.default": 0,
    "spinner.default": 0,
    "switch.default": 0,
    "table.default": 0,
    "tabs.default": 0,
    "textarea.default": 0,
    "toast.default": 0,
    "toggle-group.default": 0,
    "toggle.default": 0,
    "tooltip.default": 0,
    "ai_elements-message.default": 0,
    "attachments.default": 0,
    "chain-of-thought.default": 0,
    "checkpoint.default": 0,
    "confirmation.default": 0,
    "environment-variables.default": 0,
    "package-info.default": 0,
    "question.default": 0,
    "reasoning.default": 0,
    "sources.default": 0,
    "suggestion.default": 0,
    "task.default": 0
  },
  "pending": [],
  "skips": {
    "mic-selector.default": "The same `asChild` as `plan.default`, plus a list only one side can draw: upstream reads the microphones from `navigator.mediaDevices`, which reports nothing without a permission prompt, and a server has no such list at all. `parity-divergence.json` records the four slots this shows up on; the trigger differs by too much of its own box to compare what is painted.",
    "plan.default": "Upstream draws two elements where the reader reads one, twice over: AI Elements writes `asChild` and this shadcn build is Base UI, whose equivalent is `render`, so `<Collapsible asChild><Card>` and `<CollapsibleTrigger asChild><Button>` each nest instead of merging — and the second of those is a <button> inside a <button>, which axe reports and is right to. Taking `asChild` at its word is a decision against upstream's own page, and `parity-divergence.json` records the same one slot by slot. Every other AI Elements example gates at zero.",
    "chart.default": "Only one side can draw. `ChartContainer` puts the caller's children inside recharts' <ResponsiveContainer>, which sizes an inner wrapper to 0x0 for anything that is not a recharts element \u2014 so the reference renders the chrome and no plot, whatever the example passes it. A reference that drew a recharts <LineChart> instead would be a second design rather than a port, which parity/README.md forbids, and it was also the only example whose pixel count moved between runs. The chrome is still compared: parity.spec.mjs measures every data-slot box and 37 computed properties on both sides. This example passed at zero for as long as neither side drew: chart_style/2 returned \"\" and the <style> emitted its own source text."
  },
  "reasons": {
    "scroll-area.default": "91 px, stable across three runs, scattered over twelve lines of wrapped text rather than clustered. Markup is byte-identical, positions match to two decimals, and font smoothing, text-rendering and body classes are the same on both sides \u2014 this is glyph rasterisation, not a component difference. Budget is the measured maximum times 1.5, per PLAN.md."
  }
}
