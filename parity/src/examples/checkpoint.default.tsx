import { Checkpoint } from "@upstream/ai_elements/checkpoint";

// Ported from `StorybookWeb.Examples.checkpoint_default/1`.
export default function CheckpointDefault() {
  return <Checkpoint className="max-w-md">Reverted to the spec before the fold</Checkpoint>;
}
