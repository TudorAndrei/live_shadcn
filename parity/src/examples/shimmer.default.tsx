import { Shimmer } from "@upstream/ai_elements/shimmer";

// Ported from `StorybookWeb.Examples.shimmer_default/1`.
//
// The gradient travels across the text. Upstream moves it with `motion`, which
// calls the Web Animations API; `LiveBase.Shimmer` calls the same API with the
// same two keyframes, read off `initial` and `animate`.
export default function ShimmerDefault() {
  return <Shimmer>Reading every registry source</Shimmer>;
}
