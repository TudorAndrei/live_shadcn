defmodule LiveAiElements.Components.FileTree do
  @moduledoc """
  File tree. Built on `shadcn/collapsible`.

  Generated by `mix ui.gen` from `registry/spec/ai_elements/file-tree.json`. Every
  class string and every `data-slot` below came from upstream. Change the
  spec or the recipe, not this file.
  """

  use Phoenix.Component

  @doc "The `file_tree` part."
  attr(:default_expanded, :string, default: nil)
  attr(:expanded, :string, default: nil)
  attr(:selected_path, :string, default: nil)
  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def file_tree(assigns) do
    ~H"""
    <div role="tree" class={["rounded-lg border bg-background font-mono text-sm", @class]} {@rest}>
      <div class="p-2">
        {render_slot(@inner_block)}
      </div>
    </div>
    """
  end

  @doc "The `file_tree_icon` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def file_tree_icon(assigns) do
    ~H"""
    <span class={["shrink-0", @class]} {@rest}>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `file_tree_name` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def file_tree_name(assigns) do
    ~H"""
    <span class={["truncate", @class]} {@rest}>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `file_tree_folder` part."
  attr(:is_expanded, :string, default: nil)
  attr(:is_selected, :string, default: nil)
  attr(:name, :string, default: nil)
  attr(:path, :string, default: nil)
  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def file_tree_folder(assigns) do
    ~H"""
    <div
      data-slot={@rest[:"data-slot"] || "collapsible"}
      open={@is_expanded}
      {Map.drop(@rest, [:"data-slot"])}
    >
      <div role="treeitem" tabindex={0} class={[@class]} {@rest}>
        <div class={[
          "flex w-full items-center gap-1 rounded px-2 py-1 text-left transition-colors hover:bg-muted/50",
          if(@is_selected, do: "bg-muted", else: nil)
        ]}>
          <button
            data-slot={@rest[:"data-slot"] || "collapsible-trigger"}
            type="button"
            class="flex shrink-0 cursor-pointer items-center border-none bg-transparent p-0"
            {Map.drop(@rest, [:"data-slot"])}
          >
            <LiveShadcn.Icon.icon
              name="chevron-right"
              class={[
                "size-4 shrink-0 text-muted-foreground transition-transform",
                if(@is_expanded, do: "rotate-90", else: nil)
              ]}
            />
          </button>
          <button
            type="button"
            class="flex min-w-0 flex-1 cursor-pointer items-center gap-1 border-none bg-transparent p-0 text-left"
          >
            <.file_tree_icon>
              <LiveShadcn.Icon.icon
                :if={@is_expanded}
                name="folder-open"
                class="size-4 text-blue-500"
              />
              <LiveShadcn.Icon.icon :if={!@is_expanded} name="folder" class="size-4 text-blue-500" />
            </.file_tree_icon>
            <.file_tree_name>
              {@name}
            </.file_tree_name>
          </button>
        </div>
        <div
          data-slot={@rest[:"data-slot"] || "collapsible-content"}
          {Map.drop(@rest, [:"data-slot"])}
        >
          <div class="ml-4 border-l pl-2">
            {render_slot(@inner_block)}
          </div>
        </div>
      </div>
    </div>
    """
  end

  @doc "The `file_tree_file` part."
  attr(:is_selected, :string, default: nil)
  attr(:name, :string, default: nil)
  attr(:path, :string, default: nil)
  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:icon)
  slot(:inner_block)

  def file_tree_file(assigns) do
    ~H"""
    <div
      role="treeitem"
      tabindex={0}
      class={[
        "flex cursor-pointer items-center gap-1 rounded px-2 py-1 transition-colors hover:bg-muted/50",
        if(@is_selected, do: "bg-muted", else: nil),
        @class
      ]}
      {@rest}
    >
      <%= if @inner_block == [] do %>
        <span class="size-4 shrink-0" />
        <.file_tree_icon>
          <%= if @icon == [] do %>
            <LiveShadcn.Icon.icon name="file" class="size-4 text-muted-foreground" />
          <% end %>
          {render_slot(@icon)}
        </.file_tree_icon>
        <.file_tree_name>
          {@name}
        </.file_tree_name>
      <% end %>
      {render_slot(@inner_block)}
    </div>
    """
  end

  @doc "The `file_tree_actions` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def file_tree_actions(assigns) do
    ~H"""
    <div role="group" class={["ml-auto flex items-center gap-1", @class]} {@rest}>
      {render_slot(@inner_block)}
    </div>
    """
  end
end
