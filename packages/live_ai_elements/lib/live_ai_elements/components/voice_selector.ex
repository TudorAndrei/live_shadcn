defmodule LiveAiElements.Components.VoiceSelector do
  @moduledoc """
  Voice selector. Built on `shadcn/button`, `shadcn/dialog`.

  Upstream exports 11 more parts, each a thin
  wrapper around a part of `<.dialog>`, `<.command>`. That component is one
  function here — its parts have to agree about which one they belong to,
  and an id repeated is an id to mistype — so it is what to compose inside
  this, and there is nothing for the wrappers to wrap.

  Generated by `mix ui.gen` from `registry/spec/ai_elements/voice-selector.json`. Every
  class string and every `data-slot` below came from upstream. Change the
  spec or the recipe, not this file.
  """

  use Phoenix.Component

  @doc "The `voice_selector_gender` part."
  attr(:icon, :string, default: nil)

  attr(:value, :string,
    default: nil,
    values: [nil, "male", "female", "transgender", "androgyne", "non-binary", "intersex"]
  )

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_gender(assigns) do
    ~H"""
    <span class={["text-muted-foreground text-xs", @class]} {@rest}>
      <%= if @inner_block == [] do %>
        {@icon}
      <% end %>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `voice_selector_accent` part."
  attr(:emoji, :string, default: nil)
  attr(:value, :string, default: nil)
  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_accent(assigns) do
    ~H"""
    <span class={["text-muted-foreground text-xs", @class]} {@rest}>
      <%= if @inner_block == [] do %>
        {@emoji}
      <% end %>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `voice_selector_age` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_age(assigns) do
    ~H"""
    <span class={["text-muted-foreground text-xs tabular-nums", @class]} {@rest}>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `voice_selector_name` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_name(assigns) do
    ~H"""
    <span class={["flex-1 truncate text-left font-medium", @class]} {@rest}>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `voice_selector_description` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_description(assigns) do
    ~H"""
    <span class={["text-muted-foreground text-xs", @class]} {@rest}>
      {render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `voice_selector_attributes` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_attributes(assigns) do
    ~H"""
    <div class={["flex items-center text-xs", @class]} {@rest}>
      {render_slot(@inner_block)}
    </div>
    """
  end

  @doc "The `voice_selector_bullet` part."

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot"])
  slot(:inner_block)

  def voice_selector_bullet(assigns) do
    ~H"""
    <span aria-hidden="true" class={["select-none text-border", @class]} {@rest}>
      &bull;{render_slot(@inner_block)}
    </span>
    """
  end

  @doc "The `button` part."
  attr(:icon, :string, default: nil)
  attr(:loading, :boolean, default: nil)
  attr(:playing, :boolean, default: nil)

  attr(:size, :string,
    default: "icon-sm",
    values: ["default", "icon", "icon-lg", "icon-sm", "icon-xs", "lg", "sm", "xs"]
  )

  attr(:variant, :string,
    default: "outline",
    values: ["default", "destructive", "ghost", "link", "outline", "secondary"]
  )

  attr(:class, :any, default: nil, doc: "Appended to the class string upstream renders.")
  attr(:rest, :global, include: ["data-slot", "type", "value", "name", "formaction"])
  slot(:inner_block)

  def voice_selector_preview(assigns) do
    ~H"""
    <button
      data-slot={@rest[:"data-slot"] || "button"}
      aria-label={if(@playing, do: "Pause preview", else: "Play preview")}
      disabled={@loading}
      type="button"
      class={[
        variant_class("buttonVariants", "size", @size),
        variant_class("buttonVariants", "variant", @variant),
        "cn-button group/button inline-flex shrink-0 items-center justify-center whitespace-nowrap transition-all outline-none select-none disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 size-6",
        @class
      ]}
      {Map.drop(@rest, [:"data-slot"])}
    >
      <LiveShadcn.Icon.icon name="play" class="size-3" />{render_slot(@inner_block)}
    </button>
    """
  end

  # The variant tables, from the `cva` calls upstream writes them in.
  @variants %{
    "buttonVariants" => %{
      "size" => %{
        "default" => "cn-button-size-default",
        "icon" => "cn-button-size-icon",
        "icon-lg" => "cn-button-size-icon-lg",
        "icon-sm" => "cn-button-size-icon-sm",
        "icon-xs" => "cn-button-size-icon-xs",
        "lg" => "cn-button-size-lg",
        "sm" => "cn-button-size-sm",
        "xs" => "cn-button-size-xs"
      },
      "variant" => %{
        "default" => "cn-button-variant-default",
        "destructive" => "cn-button-variant-destructive",
        "ghost" => "cn-button-variant-ghost",
        "link" => "cn-button-variant-link",
        "outline" => "cn-button-variant-outline",
        "secondary" => "cn-button-variant-secondary"
      }
    }
  }

  defp variant_class(table, group, value), do: get_in(@variants, [table, group, value])
end
