defmodule LiveBase.Calendar do
  @moduledoc "The browser locale hook for a server-rendered calendar."

  @doc "The Phoenix hook name used by generated calendar markup."
  def hook, do: "LiveBase.Calendar"
end
